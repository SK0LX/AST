<resources>
    <!-- Base button style -->
    <style name="Widget.AST.Button" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="shapeAppearance">?attr/shapeAppearanceMediumComponent</item>
    </style>

    <!-- Dark theme button -->
    <style name="Widget.AST.DarkButton" parent="Widget.AST.Button">
        <item name="android:backgroundTint">@color/dark_black</item>
        <item name="strokeColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- Light theme button -->
    <style name="Widget.AST.LightButton" parent="Widget.AST.Button">
        <item name="android:backgroundTint">@color/white</item>
        <item name="strokeColor">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- Light theme -->
    <style name="Theme.AST" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="materialButtonStyle">@style/Widget.AST.LightButton</item>
    </style>

    <!-- Dark theme -->
    <style name="Theme.AST.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:statusBarColor">@color/dark_black</item>
        <item name="materialButtonStyle">@style/Widget.AST.DarkButton</item>
    </style>
</resources>
